---
title: "config.yml と catalog.yml の基本"
order: 4
summary: "本文の外側でビルドを支える 2 つの設定ファイルを整理し、初心者が先に押さえるべき設定項目を絞り込みます。"
audience:
  - "ビルドの前提を理解したい著者"
  - "chapterlink や secnolevel が気になる人"
  - "章追加のたびに設定で迷う人"
---

本文が書けても、`config.yml` と `catalog.yml` を触れないと本全体は動きません。この章では、初心者が最初に見るべき設定項目と、Nextpublishing 運用で気を付けたい点を分けて整理します。

## `config.yml` の最重要キー

### まずは「本の情報」と「ビルドの挙動」を分ける

`config.yml` には多くの設定項目がありますが、初学者は次の 2 つに分けて読むと理解しやすくなります。

- 書誌情報: 本のタイトル、著者、言語など
- ビルド挙動: 見出しやリンク、画像やハイライトに関する設定

### 先に見るべき設定項目

| key | まず何を見るか | 初心者への意味 |
| --- | --- | --- |
| `booktitle` | 本の題名 | 出力面の名前が分かる |
| `language` | 言語 | 日本語前提かを確認できる |
| `catalogfile` | 目次ファイルの場所 | どの `catalog.yml` を使うか分かる |
| `contentdir` | 本文の置き場 | `.re` の基準ディレクトリが分かる |

### 最小限の `config.yml` 例

最初は全部の設定を書く必要はありません。次のような、**本の題名と章の場所が分かる最小例** で十分です。

```yaml
booktitle: "はじめての Re:VIEW 本"
language: ja
catalogfile: catalog.yml
contentdir: .
```

この 4 行があると、少なくとも「どの本を」「どの目次で」「どこから読むか」が分かります。

### 「分からない設定項目は後回し」でよい

`config.yml` は深く掘ると広いですが、最初から全部理解する必要はありません。触る予定がない設定項目に時間を使うより、いまビルドに関係する設定項目だけ押さえたほうが前に進めます。

## Nextpublishing との整合に効く設定

### `chapterlink: null`

運用差分としてよく話題になる設定です。初心者は「リンク関係で不具合が出るなら、まずこの設定項目を確認する」と覚えておくと十分です。

### `secnolevel: 4`

見出し番号の扱いとつながる設定です。本文側で見出しレベルを深くしすぎると、設定との差が出やすくなります。

### `highlight` を無効にする判断

構文の色分けは便利ですが、運用との相性で問題が出ることがあります。初心者向けには、**きれいに見えるかどうかより、安定して通るか** を優先してください。

## `catalog.yml` の保守ルール

### 章追加時の更新ポイント

章追加時は、本文だけでなく `catalog.yml` にも反映が必要です。ここを忘れると「ファイルはあるのにビルドに出ない」という典型的な混乱が起きます。

### 最小限の `catalog.yml` 例

最初の 1 章だけを通すなら、`catalog.yml` は次のくらいで十分です。

```yaml
CHAPS:
  - intro.re
```

章を増やすときは、この一覧に 1 行ずつ足していきます。

### 付録と後付け部分の置き方

初心者は補足を本編の後ろへ足しがちですが、付録とあとがきを分けると構造が安定します。

- 補足資料や索引: `APPENDIX`
- あとがき、著者紹介、contributors: `POSTDEF`

### 命名は見つけやすさ重視

ファイル名は、後で見返したときに役割が分かることが大切です。番号付きかどうかより、**何の章かが分かること** を優先してください。

## ビルドとプレビューの基本フロー

### 原稿の変更から確認まで

<div class="mermaid">
flowchart TD
  A[章ファイルを更新] --> B[catalog.yml / config.yml を確認]
  B --> C[ビルドまたはプレビュー]
  C --> D[診断を確認]
  D --> E[本文 or 設定を修正]
</div>

### `config.yml` と `catalog.yml` の関係

<div class="mermaid">
flowchart TD
  A[config.yml] --> B[どの目次を使うか決める]
  B --> C[catalog.yml]
  C --> D[どの章を読むか決める]
  D --> E[章ファイル .re]
  E --> F[公開サイトや書籍の出力]
</div>

### プレビュー前に見るべきこと

ビルド前に次を確認しておくと、原因切り分けが速くなります。

1. 追加した章が `catalog.yml` に入っているか
2. 参照する画像や脚注の ID が存在するか
3. 変更した設定が本当に必要か

### 初学者向けの見かた

この 2 つの設定ファイルは、次の役割で覚えると混乱しにくくなります。

- `config.yml`: 本の全体設定
- `catalog.yml`: 章の並び順

「設定が多くて難しい」と感じたら、まずこの 2 行だけを頭に置いてください。

### サイト生成物と書籍派生物の違い

このリポジトリでは、公開サイトが主配布面です。つまり `config.yml` や `catalog.yml` の説明も、**書籍を作るためだけではなく、構造を理解して修正を速くするため** に置かれています。

## この章の終わりに

次章では、初心者が最も気になる「なぜこれが差し戻しになるのか」を、Nextpublishing の診断の観点から整理します。

## この章の根拠

- 公式仕様: Re:VIEW quickstart, `config.yml.sample`
- 実例観測: サンプル著者リポジトリの章分割と設定配置
- 運用ルール: `chapterlink`, `secnolevel`, `highlight` に関する診断群との接続
